# save.command
![Jobs倾情奉献](https://picsum.photos/1500/400 "Jobs出品，必属精品")
[toc]

## 一、功能

重新加载 bash / zsh 常见配置文件。

## 二、运行

```zsh
save
save [参数...]
```

## 三、结构约定

运行时打印的自述已经写死在 `save.command` 内部，不依赖本 README。

本 README 只用于源码浏览和维护说明。
