# c.command
![Jobs倾情奉献](https://picsum.photos/1500/400 "Jobs出品，必属精品")
[toc]

## 一、功能

进入默认 Flutter 项目，锁定 JDK 17，并执行项目检查。

## 二、运行

```zsh
c
c [参数...]
```

## 三、结构约定

运行时打印的自述已经写死在 `c.command` 内部，不依赖本 README。

本 README 只用于源码浏览和维护说明。
