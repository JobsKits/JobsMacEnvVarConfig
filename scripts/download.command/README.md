# download.command
![Jobs倾情奉献](https://picsum.photos/1500/400 "Jobs出品，必属精品")
[toc]

## 一、功能

调用 yt-dlp，自动使用默认浏览器 cookies 下载媒体。

## 二、运行

```zsh
download
download [参数...]
```

## 三、结构约定

运行时打印的自述已经写死在 `download.command` 内部，不依赖本 README。

本 README 只用于源码浏览和维护说明。
