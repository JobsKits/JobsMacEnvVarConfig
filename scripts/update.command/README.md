# update.command
![Jobs倾情奉献](https://picsum.photos/1500/400 "Jobs出品，必属精品")
[toc]

## 一、功能

通过交互菜单批量更新 Homebrew、Flutter、Node、Python、Ruby、CocoaPods 等常用开发工具链。

## 二、运行

```zsh
update
update [参数...]
```

## 三、结构约定

运行时打印的自述已经写死在 `update.command` 内部，不依赖本 README。

本 README 只用于源码浏览和维护说明。
