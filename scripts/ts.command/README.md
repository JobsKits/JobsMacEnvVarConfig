# ts.command
![Jobs倾情奉献](https://picsum.photos/1500/400 "Jobs出品，必属精品")
[toc]

## 一、功能

识别秒 / 毫秒 / 微秒 / 纳秒时间戳，并转换为本地时间。

## 二、运行

```zsh
ts
ts [参数...]
```

## 三、结构约定

运行时打印的自述已经写死在 `ts.command` 内部，不依赖本 README。

本 README 只用于源码浏览和维护说明。
